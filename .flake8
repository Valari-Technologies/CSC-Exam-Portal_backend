[flake8]
max-line-length = 120
# E203 (whitespace before ':') and W503 (line break before binary operator)
# conflict with black's formatting — disable per black's recommendation.
extend-ignore = E203, W503
exclude =
    .git,
    __pycache__,
    migrations,
    venv,
    staticfiles,
    settings
max-complexity = 12
